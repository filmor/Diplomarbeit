\iflanguage{ngerman}{
  Sei für $V,W\in\Cinf{(0,1)}$
  \begin{equation*}
    \Delta_\lambda := -\partial_x^2 + \lambda^2 V(x) + W(x)
  \end{equation*}
  ein Differentialoperator auf $\Cinf((0,1))$ und $\mu(x) :=
  \sqrt{\lambda^2V(x)+z^2}$. Dann hat der Kern der Resolvente $(\Delta_\lambda
  + z^2)^{-1}$ von $\Delta_\lambda$ die folgende gemeinsame asymptotische
  Entwicklung in $\lambda$ und $z$:
}{
  For $V,W\in\Cinf((0,1))$ let
  \begin{equation*}
    \Delta_\lambda := -\partial_x^2 + \lambda^2 V(x) + W(x)
  \end{equation*}
  be a differential operator on $\Cinf((0,1))$ and $\mu(x) :=
  \sqrt{\lambda^2V(x) + z^2}$. Then the kernel of the resolvent
  $(\Delta_\lambda^2)^{-1}$ of $\Delta_\lambda$ has the following common
  asymptotic expansion in $\lambda$ and $z$:
}
\begin{align*}
  \input{snippets/formula-interior}.
\end{align*}
